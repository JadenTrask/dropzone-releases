import {sightSolution} from './wardogs-sight.js';
import {bearingText,milText} from './wardogs-model.js';
export function gunnerSight(shot,arc,changed=false){
 const solution=sightSolution({shot,arc,mapChanged:changed}),ready=solution.status==='flat',bearing=ready?shot.azimuth:0;
 const ticks=Array.from({length:17},(_,i)=>{const offset=(i-8)*5,x=300+offset*5,degrees=(Math.round(bearing/5)*5+offset+360)%360;return `<path d="M${x} 55v${i%3===0?-14:-7}"/>${i%3===0?`<text x="${x}" y="30">${degrees}°</text>`:''}`;}).join('');
 return `<svg viewBox="0 0 600 440" role="img" aria-label="Gunner sight: ${ready?Math.round(shot.distance)+' metres, '+bearingText(shot.azimuth)+', '+milText(solution.command)+' MIL':'Place an in-range gun and target'}"><rect width="600" height="440" rx="8" fill="#100a06"/><g fill="#ffb178" stroke="#c77740" stroke-width="1.5" font-family="monospace" font-size="14" text-anchor="middle"><path d="M80 55H520"/>${ticks}<path d="M300 60l-7 12h14z" fill="#ffb178"/><circle cx="300" cy="235" r="62" fill="none"/><circle cx="300" cy="235" r="7" fill="none"/><path d="M300 135v30m0 140v30M200 235h30m140 0h30M288 205h24m-24 60h24M80 175h24m-24 60h24m-24 60h24M496 175h24m-24 60h24m-24 60h24"/><text x="55" y="100" stroke="none">RNG</text><text x="545" y="100" stroke="none">MIL</text><text x="60" y="240" stroke="none">${ready?Math.round(shot.distance):'—'}</text><text x="540" y="240" stroke="none">${ready?milText(solution.command):'—'}</text><text x="300" y="375" font-size="30" stroke="none">${ready?Math.round(shot.distance)+' M':'NO SOLUTION'}</text><text x="300" y="405" stroke="none">${ready?milText(solution.command)+' MIL · '+bearingText(shot.azimuth):'Place gun and target within range'}</text></g></svg><p class="wd-gunner-note">Flat-ground game table · terrain clearance not evaluated</p>`;
}
