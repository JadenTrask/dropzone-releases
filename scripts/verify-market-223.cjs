const {chromium}=require('C:/Users/Jaden/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({channel:'msedge',headless:true}),p=await b.newPage({viewport:{width:1440,height:1000}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.goto('http://localhost:4174/?game=market&context=wardogs');await p.waitForSelector('#market-graph');assert.match(await p.locator('.market-stats').innerText(),/2,067,814/);await p.locator('[data-range="7"]').click();assert.match(await p.locator('.market-readout').innerText(),/7 daily/);assert.equal(await p.locator('#market-date').count(),0);assert.equal(await p.locator('.market-axis-date').count(),7);await p.locator('#market-graph').focus();await p.keyboard.press('Home');assert.match(await p.locator('#market-point').innerText(),/2026-09-08/);await p.locator('#market-refresh').click();await p.waitForFunction(()=>!document.querySelector('#market-refresh').disabled);assert.equal(await p.locator('[data-range="7"]').getAttribute('aria-pressed'),'true');await p.locator('[data-range="all"]').click();assert.match(await p.locator('.market-readout').innerText(),/532 daily/);const rect=await p.locator('#market-graph').boundingBox();await p.mouse.move(rect.x+rect.width*.5,rect.y+rect.height*.5);assert.equal(await p.locator('#market-hover').getAttribute('visibility'),'visible');assert.match(await p.locator('#market-tooltip-price').textContent(),/\$/);await p.screenshot({path:'docs/screenshots/market-223.png',fullPage:true});assert.deepEqual(errors,[]);await b.close();console.log('Market chart, ranges, keyboard inspection, refresh preservation: passed');})();
